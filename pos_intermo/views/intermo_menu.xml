<!-- <odoo>
    <record id="action_intermo_settings" model="ir.actions.act_window">
        <field name="name">Intermo Settings</field>
        <field name="res_model">intermo.settings</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_intermo_settings_form"/>
        <field name="target">current</field>
        <field name="context">{'default_name': 'Intermo Settings'}</field>
    </record>

    <menuitem id="menu_intermo_root" name="Intermo Gateway API Key Configuration" sequence="10"/>
    <menuitem id="menu_intermo_settings" name="Intermo Settings" parent="menu_intermo_root" action="action_intermo_settings" sequence="10"/>
</odoo> -->
